The class for this set of code is labeled as Cosplay with private attributes ID, name, and price which are int, string and float variables respectively. 
The public attribute defines and assigns variables to the class with messages displayed for Constructor and Destructor when the code is ran.
It then displays the details of the class in a specific format.
In main, the class is written with details for the variables of said class.
The program is run and the first message, "Constructor called" is displayed.
Then inputs of the the details for the above class in main are then displayed.
Finally, the last message, "Destructor called" is displayed before the code ends.
