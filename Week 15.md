The class is saved in the header file where different data points are saved as string values each with different names or labels.
It then prints out the details singaled out by the string values in the format that has been set up for output of the information.
The main code starts by opening the file where all the bat information is stored in a csv.
It then starts pulling information from the csv file and displays the information according to the format mentioned above.
As this starts, the code also starts counting from 0. 
It counts up 1 digit everytime a full line of information has been displayed and stops once 15 lines from the csv file have been outputted and displayed.
