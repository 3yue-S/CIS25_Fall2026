When the code runs, it creates a class called BankAccount and is named myAccount.
Then, 200.00 is set to be deposited. This number is labeled as amount, which is equated to the balance at this point in time.
The window then displays the text "Added starting balance of " output of the current balance amount.
This number is then inputted into the file titled "transactions.txt", where it is saved as an amount attached to the type named "Deposit".
After that, the code says it makes a purchase with the output of the item and cost, with just the numbers.
The code then jumps to the makePurchase set of code, where it checks if the cost is more than the balance in whioch case the code outputs the text "Insufficient funds for " item.
Otherwise, the cost is deducted from the balance and the code outputs the text "Purchased " item.
It then jumps to the saveTransaction set of code, where it opens the file titled "transactions.txt" and saves the cost of this purchase as "Purchase" along with a dollar sign.
The file is then closed and the code jumps to the next line of code in main, which is another purchase, this time a book.
The code then jumps to the makePurchase set of code, where it checks if the cost is more than the balance in whioch case the code outputs the text "Insufficient funds for " item.
Otherwise, the cost is deducted from the balance and the code outputs the text "Purchased " name of the item.
It then jumps to the saveTransaction set of code, where it opens the file titled "transactions.txt" and saves the cost of this purchase as "Purchase" along with a dollar sign.
The file is then closed and the code jumps to the next line of code in the main set of code, which is another purchase, this time it is Game Skins and Cosmetics.
The code then jumps to the makePurchase set of code, where it checks if the cost is more than the balance in whioch case the code outputs the text "Insufficient funds for " item.
Otherwise, the cost is deducted from the balance and the code outputs the text "Purchased " name of the item.
It then jumps to the saveTransaction set of code, where it opens the file titled "transactions.txt" and saves the cost of this purchase as "Purchase" along with a dollar sign.
The file is then closed and the code jumps to the next line of code in the main code, which tells it to display the current balance.
The code then jumps to another void set of code, labeled displayBalance.
It then outputs the text "Current Balance: $ " current balance amount after the all the purchase amount has been subtracted and closes the file "transactions.txt".
The main set of code then ends and returns to the initial state.
